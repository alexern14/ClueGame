/*
 * Authors: Alexandra Ernst & Mia Belliveau
 * 
 * This enum class will give an enum value to doors to tell which way they open
 */

package clueGame;

public enum DoorDirection {
	UP(), DOWN(), LEFT(), RIGHT(), NONE();
}
